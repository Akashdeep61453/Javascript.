// if
const isUserLoggedIn = true;
if(2!==2){
console.log(`Hi`);
}
// <,>, <=, >-, ==, !=, !== 
// 2==="2" also checks the datatype 2 !== "2"

const score = 200
if(score > 100){
    // const power = "fly"// error with const
    var power = "fly"
    console.log(`User power: ${power}`);
}
 console.log(`User power: ${power}`);


 const balance = 1000
 // different scenarios of if and else if etc
 // also of && and ||
